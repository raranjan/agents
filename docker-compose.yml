services:
  research:
    build:
      context: .
      dockerfile: infrastructure/docker/research.Dockerfile
    ports:
      - "${PORT:-8000}:8000"
    environment:
      LOG_LEVEL: ${LOG_LEVEL:-INFO}
      APP_NAME: ${APP_NAME:-research-agent}
    volumes:
      - ./platform:/app/platform
      - ./agents:/app/agents
    command: uvicorn agents.research.app:app --host 0.0.0.0 --port 8000 --reload
